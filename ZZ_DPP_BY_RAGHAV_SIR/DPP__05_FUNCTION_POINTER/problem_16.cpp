/*

Q16. State True or False for each statement below regarding C++ Functions and Pointers.


(i) Default arguments in a C++ function must always be provided from right to left.

. . . . . . . . . . .TRUE

(ii) The statement int* p = &x; stores the value of variable x directly inside p.

. . . . . . . . . . .FALSE

(iii) Function overloading allows you to have two functions with the exact same name and parameter list, as long as their return types are different.

. . . . . . . . . . .FALSE

(iv) Using the scope resolution operator ::x allows you to access a global variable x even if a local variable x is shadowing it.

. . . . . . . . . . .TRUE

(v) A double pointer int** p is used to store the address of a regular integer variable directly.

. . . . . . . . . . .FALSE

(vi) The ampersand operator (&) is used to get the memory address of a variable.

. . . . . . . . . . .TRUE

(vii) The inbuilt math function sqrt(25) returns an integer data type by default in C++.

. . . . . . . . . . .// FALSE

*/

#include<iostream>
using namespace std;
int main(){
  
}
