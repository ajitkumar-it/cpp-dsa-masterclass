/*
Q3. What internally happens when a std::vector capacity is full and a new element is added using push_back()?

             A. The program throws an overflow error.     
             B. The program simply overwrites the last element.  
             C. The vector automatically allocates a new block of memory (usually double the size) and moves elements.     
             D. The capacity strictly increases by exactly 1 element.                     

*/