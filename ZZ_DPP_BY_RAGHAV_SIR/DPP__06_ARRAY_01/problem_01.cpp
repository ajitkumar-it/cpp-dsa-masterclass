/*
Q1. State True or False for each statement below regarding Arrays and Vectors.


(i) The size of a static array in C++ can be dynamically changed during runtime after its declaration.. . . . . . . . . . .


(ii) Attempting to access arr[-1] in C++ will always trigger a direct compilation error.. . . . . . . . . . .


(iii) The expression sizeof(arr)/sizeof(arr[0]) accurately calculates the number of elements in a static array.. . . . . . . . . . .


(iv) When an array is passed to a function using void solve(int arr[]), it is fundamentally passed by reference (as a pointer).. . . . . . . . . . .


(v) The statement vector<int> v(5, 10); initializes a vector of size 5, where every element holds the value 10.. . . . . . . . . . .


(vi) The capacity() of a vector in C++ is always strictly equal to its size(). . . . . . . . . . . .


(vii) Elements of a standard C++ array are allocated in contiguous memory locations.. . . . . . . . . . .

*/